import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  btnToTop: {
    width: 20,
    height: 10,
  },
  listStyle: {
    marginBottom: 15
  }
});

export default styles;
